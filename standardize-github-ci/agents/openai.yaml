interface:
  display_name: "Standardize GitHub CI"
  short_description: "Audit and optimize repository CI safely"
  default_prompt: "Use $standardize-github-ci to audit this repository’s GitHub Actions and propose an approval-gated standardization plan."
