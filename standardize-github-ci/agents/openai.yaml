interface:
  display_name: "Standardize GitHub CI"
  short_description: "Audit and gate exact-head CI after agent review"
  default_prompt: "Use $standardize-github-ci to audit and, with approval, implement agent-gated exact-head GitHub CI while keeping out-of-band review state outside GitHub."
